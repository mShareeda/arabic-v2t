import { Alexandria } from 'next/font/google'

/**
 * Alexandria — الخط الوحيد في التطبيق.
 *
 * عائلة متغيّرة (100–900) تغطي العربية واللاتينية معًا، فتغني عن الجمع بين
 * خطّين مختلفين لكل نص، ويبقى الوزن والارتفاع متناسقين عند خلط اللغتين داخل
 * السطر الواحد — وهو أمر متكرر هنا بحكم دعم تبديل اللغة وسط الجملة.
 *
 * يُستضاف ذاتيًا عبر `next/font` لا عبر وسم `<link>` إلى Google:
 * فلا طلب خارجي يحجب أول رسم للصفحة، ولا وميض للخط الاحتياطي قبل وصوله،
 * ولا اعتماد على إمكانية الوصول إلى نطاق Google من بلد المستخدم.
 */
export const alexandria = Alexandria({
  subsets: ['arabic', 'latin'],
  variable: '--font-alexandria',
  display: 'swap',
  // خط احتياطي يُعدَّل مقاسه ليطابق مقاييس Alexandria، فلا تقفز الصفحة
  // عند استبداله بالخط الحقيقي
  adjustFontFallback: true,
})
